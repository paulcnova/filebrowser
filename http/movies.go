package fbhttp

import (
	"net/http"
)

func movieListGetHandler() handleFunc {
	return func(res http.ResponseWriter, req *http.Request, data *data) (int, error) {
		// file, err := files.new
		return 0, nil
	}
}
